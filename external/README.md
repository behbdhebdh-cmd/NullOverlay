# External Dependencies

CMake can consume dependencies from this folder if you place them here:

- `external/imgui` containing `imgui.h` and `backends/`
- `external/minhook` containing `include/MinHook.h` and `bin/MinHook.x64.lib`

The current workspace already includes compatible vendor drops at the repository root, so copying files here is optional.
